[pytest]
markers =
    otp: live OTP flow; requires an authorized test account and inbox adapter
testpaths = tests
addopts = -ra --browser-channel=chrome --screenshot=on --output=test-results --tracing=retain-on-failure
